FROM python:3.11-slim

WORKDIR /app

# Copy everything
COPY . .

# Install requirements
RUN cd backend && pip install --no-cache-dir -r requirements.txt

# Set environment
ENV PYTHONUNBUFFERED=1

# Run bot from root
CMD ["python", "-m", "backend.bot_runner"]
